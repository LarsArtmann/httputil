# Status Report — 2026-09-23 16:22 CEST — art-dupl Deduplication (`cors_ratelimit_specs_test.go`) + Duplication-Baseline Refresh

**Session scope (per owner instruction):** this report covers only the current session's run — the `art-dupl -t 5` clone group ("deduplicate!"), its fix, verification, the AGENTS.md baseline refresh, and the follow-up archaeology done for this status pass. No repo-wide research beyond the directly-adjacent prior reports (21-46, 15-07) that the grep in this pass surfaced.

**Session task:** `art-dupl --sort total-tokens -t 5 --type-aware` reported 1 shown group — 6 clones in `httpspec/cors_ratelimit_specs_test.go` (loop-style find-spec-by-name blocks). Owner asked: "deduplicate!".

**Ending state:** refactor committed (daemon `33b1818`, content-pointer only), all gates that were run are green, `art-dupl -t 5` → 0 shown groups, AGENTS.md baseline re-measured and rewritten. Two overconfident claims made mid-session are corrected below (§d).

---

## a) FULLY DONE

| #  | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Evidence                                                                                                                              |
| -- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| a1 | Clone group reproduced: 6 clones, all the same "loop over spec collection → skip non-matching → check → return → t.Fatal" block, at lines 112/137/507/657/681/729                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | User-provided output + local re-run (identical: 1 shown group, 15 non-actionable, 33 filtered suppressed)                             |
| a2 | Judgment: **harmful duplication, extract** — the file already had the `(Spec, bool)` finder idiom (`findPrivateNetworkSpec`, `findOriginMatchesRequestedSpec`); the 6 flagged sites were the un-refactored older shape, and 13 loop-style sites + 7 finder call sites shared the same find+fatal boilerplate                                                                                                                                                                                                                                                                                                                                   | File read end-to-end (741 lines); 18 `for _, spec := range` sites enumerated, 3 are `t.Run`-over-all loops (kept — different pattern) |
| a3 | **Refactor executed:** added `findSpec(specs []Spec, name string) (Spec, bool)` + `lookupSpec(t, specs, name) Spec` (with `t.Helper()` per `thelper`) at `httpspec/cors_ratelimit_specs_test.go:61-83`; converted all 20 sites; deleted both per-collection finders; every test kept its own distinct failure message (name = claim convention)                                                                                                                                                                                                                                                                                                | Daemon commit `33b1818` (verified: 22 `lookupSpec` occurrences, 0 occurrences of either deleted finder); `multiedit` 19 edits applied |
| a4 | **Verification battery green:** `go test -race ./...` (root module incl. `httpspec`); `go test -race -count=10 ./httpspec/...` (AGENTS.md paralleltest protocol); `golangci-lint run ./...` → 0 issues; `go vet` clean; `gofmt` clean after `golangci-lint fmt`                                                                                                                                                                                                                                                                                                                                                                                | All commands executed this session, all exit 0                                                                                        |
| a5 | **Detector re-verified after refactor:** `-t 5` → **0 shown groups** (was 1 group / 6 clones); `-t 2` → 3 shown (all production-file micro-pairs, none in the changed file); `-t 1` → 5 shown (the accepted `Middleware` alias pair + `scripts/doc-snippet-refs` `if err != nil` pair + the same 3 micro-pairs)                                                                                                                                                                                                                                                                                                                                | `art-dupl 0.7.0-81ce00b`, runs at 16:0x CEST this session                                                                             |
| a6 | **AGENTS.md duplication baseline refreshed:** Commands-block comment + §Accepted Code Duplication rewritten with measured 2026-09-23 numbers (art-dupl version recorded) and the 3 production micro-pairs dispositioned **accept** with per-item rationale (2-3-line pairs; extraction cost ≥ benefit: csrf all-or-nothing parse exits ARE the documented contract; `flushHeader` prologue is the stdlib ResponseWriter idiom; negotiator scanner loops are already the named extraction over different predicates)                                                                                                                            | AGENTS.md diff (8 insertions, 4 deletions), working tree; `nix fmt` (treefmt) → 0 changed                                             |
| a7 | **Attribution archaeology (this status pass):** the 3 production micro-pairs provably **predate** yesterday's clean `-t 2` run — `csrf.go` pair introduced `6b684ec` (09-10 08:55), `server_timing.go` prologue `293e63b` (08-07), negotiator scanner loops `161114c` (06-14) — while the 2026-09-22 21:46 report verified "-t 2 → 0 groups" that same night (123 files). Therefore the floor's death is **tool/invocation drift, not code regression**; the cors test-file loops existed since ≤ 09-10 (`2cfa29a`) / 14:33 today (`22574bc`) and were invisible to yesterday's floor because that claim was computed with test files excluded | `git log -S` / `--follow` outputs recorded above; docs/status/2026-09-22_21-46 report §a5                                             |

## b) PARTIALLY DONE

| #  | What works                                                                                                                                                                     | What remains                                                                                                                                                                                                                                                                                                                                                     | Effort |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| b1 | AGENTS.md baseline measured + rewritten; conclusion ("old claim no longer reproduces") is verified                                                                             | The attribution sentence was written **before** the a7 archaeology existed. It happens to be the best-supported hypothesis, but the binary/invocation used by the 21-46 session is still unknown (art-dupl version not recorded there; no version pin exists in the repo). Wording could be softened to "pending version pin" — or the pin from f3 makes it moot | S      |
| b2 | Pre-tag art-dupl sweep (TODO_LIST release item; 15-07 report item 12 planned exactly this re-run): this session ran `-t 5`, `-t 2`, `-t 1`                                     | The documented sweep is `-t 2..25` plus `-t 1` with the result recorded; thresholds 3-4 and 6-25 not run this session. Also required **on the exact release tag commit** per RELEASE.md                                                                                                                                                                          | S      |
| b3 | Root-module gates all green this session                                                                                                                                       | `server_timing` per-module gate (`cd server_timing && go test -race ./... && golangci-lint run`) not run — module untouched, risk ≈ 0, but the documented per-module gate was skipped                                                                                                                                                                            | S      |
| b4 | Deliberate-skip list, unverified nonetheless: erraudit real gates, full `buildflow --build-mode dev`, `nix flake check` (only `nix fmt`/treefmt ran), `go test -bench=. ./...` | Justification: no production/error-path/config changes this session (test file + markdown only). Still: unverified is unverified                                                                                                                                                                                                                                 | S each |

## c) NOT STARTED

| #  | What                                                                                                                                                                      | Why not started                                                                                  | Still wanted?        |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | -------------------- |
| c1 | Annotate the 21-46 report's §a5 ("`-t 2` → 0 groups, floor intact") as superseded by this session's measurement (docs-health ANNOTATE mode, non-destructive)              | Discovered only during this status pass; user instructed report-then-wait                        | Yes — S              |
| c2 | Audit the **33 filtered-suppressed** clone groups from the original `-t 5` run (and 136 at `-t 2`) — never eyeballed by anyone; we trust art-dupl's default filters       | Out of session scope; owner risk-appetite question (§g Q3)                                       | Pending Q3           |
| c3 | Compile-time alias-identity test (`var _ httputil.Middleware = servertiming.Middleware(nil)`) — 21-46 report §c2, still open                                              | Not this session's task                                                                          | Yes — S              |
| c4 | ROADMAP open question "art-dupl `-t 1` baseline policy" (raised 09-22): today's AGENTS.md rewrite records the observed baseline but does **not** decide the gate question | Owner-gated                                                                                      | Yes — pending Q1/Q2  |
| c5 | `docs-health` HARVEST of this report's §f into `TODO_LIST.md` / `ROADMAP.md`                                                                                              | Skill says run when the session continues; user said write-then-wait                             | Yes — on resume      |
| c6 | CHANGELOG entry question for this session's changes (test-only refactor + doc refresh)                                                                                    | ROADMAP open question "CHANGELOG granularity for doc-only changes" is unresolved; no entry added | Pending owner ruling |

## d) TOTALLY FUCKED UP

| #  | What                                                                                                                                                                                                                                                                                                                                                                                                                        | Severity                                                   | Root cause                                                                                                                      | Mitigation                                                                                                     |
| -- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| d1 | **I wrote the AGENTS.md attribution ("no longer reproduces with current tooling") as an assertion before having evidence for it.** The measured numbers were real; the _cause_ was a guess. The a7 archaeology (done only now, in this status pass) supports it — but write-then-verify is exactly backwards for a load-bearing doc claim, and at edit time I could not have defended it                                    | Medium (doc trust; wrong docs are worse than missing docs) | I conflated "I observed different output" with "I know why"                                                                     | Evidence chain now recorded (§a7). Options: soften the AGENTS.md wording, or make it moot via f3 (version pin) |
| d2 | **My previous-turn summary contained a false claim:** "the three production micro-pairs date from post-baseline work (csrf.go touched today)". File-touch ≠ clone-introduction; `git log -S` shows the csrf pair landed 09-10. The claim was repeated to the owner before verification and is corrected here                                                                                                                | Low-Medium (misinformation, since corrected)               | Reasoned from mtime/commit-date adjacency instead of running `-S` first                                                         | This report is the correction of record                                                                        |
| d3 | **The repo's duplication "floor" was never a gate — it was a vibe.** "-t 2..25 = 0 groups" was asserted in AGENTS.md with no detector version pin and no recorded invocation, so yesterday's session could verify it "intact" and today's session could measure it broken with no way to tell which side moved. My session papered over the mechanism hole by re-measuring instead of fixing it (pin + recorded invocation) | Medium (process; recurs every time the tool updates)       | Baseline claims tied to an unpinned binary                                                                                      | f3/f4; my AGENTS.md rewrite at least records version + flags with the claim                                    |
| d4 | **Series continuity missed during the dedup session.** Two directly-relevant prior reports existed (21-46: the art-dupl acceptance episode; 15-07, same day, whose item 12 _predicted my exact task_). I found them only during this status pass and re-derived dispositions (e.g. nearly re-litigating the t2 micro-pairs) without them                                                                                    | Low (wasted context, risk of contradicting prior rulings)  | The deduplicate-code skill mandates iteration, not series-reading; I didn't grep `docs/status/` for "art-dupl" before executing | e3 below; 30-second pre-flight grep                                                                            |

Honest accounting beyond d1-d4: no production code changed, no test behavior changed (assertions byte-identical, only acquisition restructured), no data loss, no reverts, working tree clean. The changed file is daemon-committed (`33b1818` — cited as content pointer only, per the parallel-writer protocol).

## e) WHAT WE SHOULD IMPROVE

1. **Pin the detector, then claim baselines.** `art-dupl` should be version-pinned (flake.nix) and every baseline claim should carry `version + flags + file count` on one line. Until then, "anything NEW at either threshold is a finding" is undecidable — d3.
2. **Archaeology before attribution.** `git log -S` for _when the clone appeared_ before writing _why the baseline moved_ into AGENTS.md. Cost of skipping it this session: d1 + d2.
3. **Grep the series before executing a known-named task.** `rg -i "art-dupl" docs/status/` takes 30 seconds and would have surfaced 15-07 item 12 (this exact re-run) and the 21-46 dispositions. Candidate lesson for project AGENTS.md (or `lessons.md` in crush-config, by commit).
4. **Resolve the test-file-exclusion ambiguity.** AGENTS.md's old parenthetical said the 0-groups claim was computed "with test files auto-excluded"; today's default run _shows_ a test-file group. Decide (and document) whether baseline runs include test files — otherwise the next baseline refresh re-litigates this.
5. **Keep the accepted-clone discipline I applied today:** every accepted clone gets a bullet with file:line + why-extraction-is-worse. The list is the only thing that makes "accept" durable across sessions.
6. **Test coverage was not re-measured** after restructuring 20 test sites (no production change, so FEATURES.md's coverage table is not stale — but a `-coverprofile` re-run is 2 minutes of insurance if the release sweep runs anyway).
7. **Self-review Q10 (split brains):** one found — the 21-46 report's §a5 now carries a superseded claim, unannotated (c1). No other doc repeats the baseline claim (grep verified: only AGENTS.md + frozen history). Self-review Q7 (ghost systems): none — `findSpec`/`lookupSpec` have 22 wired call sites; nothing orphaned. Q9 (removed something useful?): the two deleted finders' _names_ are gone but their behavior is preserved via `lookupSpec`; the deleted `found`-flag and `ok`-bool boilerplate was pure duplication.

## f) Top-N things to get done next (brainstorm, not commitment — HARVEST fuel)

Session-derived, sorted by impact. Impact / Effort (S <30min, M 30min-2h, L >2h) / Category.

| #  | Task                                                                                                                                                                                                                             | Impact   | Effort | Category      |
| -- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | ------------- |
| 1  | Rule `v1.3.1` vs `v1.4.0` and cut the release per RELEASE.md (TODO_LIST High #1; `[Unreleased]` carries the etagmetrics removal + Go 1.27.1 floor)                                                                               | Critical | M      | Release       |
| 2  | Pre-tag sweep: full `art-dupl -t 2..25` + `-t 1`, result recorded with version + flags, on the exact tag commit                                                                                                                  | High     | S      | Quality       |
| 3  | Pin `art-dupl` in flake.nix (or record binary hash) so duplication baselines become enforceable; record the sanctioned version in AGENTS.md                                                                                      | High     | S      | Quality       |
| 4  | Make the pre-tag sweep mechanical: expected-residue check ("no NEW groups vs the accepted list") in buildflow (21-46 item 11, Q2-blocked)                                                                                        | Medium   | M      | Quality       |
| 5  | Re-run erraudit real gates (`legacy_as`, `stdlib_constructor`) post-refactor — expected zero deltas, closes b4                                                                                                                   | Medium   | S      | Quality       |
| 6  | Run `server_timing` per-module gate (`go test -race ./... && golangci-lint run` from `server_timing/`) — closes b3                                                                                                               | Medium   | S      | Quality       |
| 7  | `nix flake check` after today's AGENTS.md + test edits (treefmt gate proven stale-binary-sensitive on 09-23)                                                                                                                     | Medium   | S      | Quality       |
| 8  | Full `buildflow --build-mode dev` run; triage only NEW finding classes per AGENTS.md                                                                                                                                             | Medium   | M      | Quality       |
| 9  | Annotate 21-46 §a5 as superseded by the 2026-09-23 measurement (docs-health ANNOTATE; closes the one live split brain, §e7)                                                                                                      | Medium   | S      | Documentation |
| 10 | Audit the 33 filtered-suppressed groups (and spot-check 136 at `-t 2`) or explicitly document trust in the default filters                                                                                                       | Medium   | M      | Quality       |
| 11 | Owner ruling on accept-vs-extract for the 3 production micro-pairs (my vote: accept as documented; §g Q1)                                                                                                                        | Medium   | S      | Quality       |
| 12 | Grep root-package `*_test.go` for the loop-find pattern — this session's grep covered `httpspec/` only                                                                                                                           | Low      | S      | Quality       |
| 13 | Soften AGENTS.md attribution wording to "pending version pin" if f3 is declined                                                                                                                                                  | Low      | S      | Documentation |
| 14 | Decide ROADMAP open question: `-t 1` baseline gate policy (§g Q2)                                                                                                                                                                | Medium   | S      | Decision      |
| 15 | Decide CHANGELOG granularity for doc-only/test-only changes (ROADMAP open; determines whether this session needs an entry)                                                                                                       | Low      | S      | Decision      |
| 16 | Record the "grep the series before executing" lesson in project AGENTS.md (or crush-config `lessons.md` by commit)                                                                                                               | Medium   | S      | Process       |
| 17 | Document whether baseline runs include test files (resolves the §e4 ambiguity)                                                                                                                                                   | Low      | S      | Documentation |
| 18 | Compile-time alias-identity test for the accepted `Middleware` aliases (21-46 §c2)                                                                                                                                               | Medium   | S      | Quality       |
| 19 | Policy for the two identical doc comments above the `Middleware` aliases (21-46 item 13)                                                                                                                                         | Low      | S      | Documentation |
| 20 | `go test -race -count=10 ./...` full-repo (not just `httpspec/`) as pre-tag insurance                                                                                                                                            | Low      | S      | Quality       |
| 21 | Re-measure coverage after the test restructure; confirm FEATURES.md coverage-gap table untouched-correct                                                                                                                         | Low      | S      | Quality       |
| 22 | `go test -bench=. ./...` smoke run (no benchmarks touched; cheap insurance before the tag)                                                                                                                                       | Low      | S      | Quality       |
| 23 | Verify `docs/architecture-reference.md` needs no dedup-session update (export tables shouldn't — test helpers aren't exports; confirm and close)                                                                                 | Low      | S      | Documentation |
| 24 | Confirm FEATURES.md needs no update for this session (no production change — expected no-op)                                                                                                                                     | Low      | S      | Documentation |
| 25 | Consider a one-line pointer comment at each accepted production pair → AGENTS.md §Accepted (or reject as comment noise — decide)                                                                                                 | Low      | S      | Quality       |
| 26 | `lookupSpec` fatal message: current `spec %q not found` drops the collection name the old per-test fatals had ("not found in CORSSpecs") — dead-branch text, but decide if the collection param is worth restoring (my vote: no) | Low      | S      | Quality       |
| 27 | Sweep for other copy-paste find+fatal shapes in `httpspec` beyond spec lookup (e.g. repeated handler-closure shapes) — likely nothing at actionable thresholds                                                                   | Low      | S      | Quality       |
| 28 | `t.Run`-loop tests (lines 85/334/355 post-refactor): consider a shared subtest runner — likely **reject** (distinct messages, table-driven tests are banned by convention)                                                       | Low      | S      | Quality       |
| 29 | art-dupl HTML output was "CSS soup" in a 07-26 session and I used text mode — check if 0.7.0 HTML is fixed; if yes, skill docs can recommend it again                                                                            | Low      | S      | Tooling       |
| 30 | Delete-or-keep decision for the `mw1`/`mw2` and `newTypedBodyHandler` accepted test clones is already recorded — nothing to do; listed so HARVEST doesn't re-open them                                                           | —        | —      | —             |
| 31 | Add the 3 accepted micro-pairs to the next docs-health VERIFY pass inputs (they are new AGENTS.md claims — verify file:line refs still match after future refactors)                                                             | Low      | S      | Documentation |
| 32 | Sweep AGENTS.md Commands block for other unpinned-tool baseline claims (same failure mode as d3 likely applies to erraudit advisory counts — the "45+41" numbers are dated 2026-09-15)                                           | Medium   | S      | Quality       |
| 33 | Pre-tag: confirm `go.work` + `GOWORK=off` builds still resolve from module cache after today's commits (vendor removal invariant)                                                                                                | Low      | S      | Quality       |
| 34 | Pre-tag: verify no version-directive downgrades snuck into `go.mod` (the 09-23 daemon-casualty class) — `git diff` go directives against the last tag                                                                            | Medium   | S      | Quality       |
| 35 | Mark 15-07 item 12 (art-dupl baseline re-run) as partially executed by this session (ANNOTATE sweep will hit it; give it the cross-reference)                                                                                    | Low      | S      | Documentation |

**Adjacent threads noticed while cross-referencing (owned by other reports — listed so they don't get lost, not re-planned here):** the remaining 15-07 items (release cut = item 11 ≙ f1), the six standing ROADMAP owner questions (trash-mover identity, daemon-window commit flow, go-etag go-directive, tracked root binaries, `AbsentEncodingFirstConfigured` lifecycle, go.work membership — 13-26 report), and the ~30-added-tests plan from 15-07 that likely created today's 14:33 test-file commit.

## g) Questions I cannot answer myself

1. **Accept or extract the 3 production micro-pairs?** They break the historically-zero `-t 2` floor. My judgment: extraction is strictly worse for all three (2-3-line pairs; the csrf pair _is_ the documented all-or-nothing contract). Do you concur with grandfathering them into the accepted list, or do you want a strict zero floor even at the cost of worse code?
2. **Should the detector be pinned?** Is version-pinning `art-dupl` in flake.nix (and recording version+flags with every baseline claim) the sanctioned mechanism, or do you accept periodic re-measurement with drift acknowledged? This decides whether d3 gets a mechanism fix or stays a documentation convention.
3. **Trust the filters?** art-dupl's default filters suppressed 33 groups at `-t 5` (136 at `-t 2`) that no session has ever eyeballed, as far as this series shows. Audit them once (M), or explicitly declare the default filters trusted?

---

_Prepared per the status-report skill (sections a-g) with the brutal-self-review questions answered inline (§e7, §d). Format override honored per owner instruction: Markdown at `docs/status/` instead of the skill's canonical HTML dashboard. §f is HARVEST fuel — it should reach `TODO_LIST.md`/`ROADMAP.md` when the session resumes (c5)._
