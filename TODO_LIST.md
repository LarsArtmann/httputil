# TODO List — httputil

Short- and mid-term improvement tasks. Each item verified against the actual code. Completed work lives in [CHANGELOG.md](CHANGELOG.md) (`[Unreleased]` and the frozen version sections); rejected ideas live in [ROADMAP.md](ROADMAP.md) Non-goals; process decisions live in [docs/DECISION_LOG.md](docs/DECISION_LOG.md).

_Updated: 2026-09-23 (docs-health audit: v1.2.0 + v1.3.0 found already tagged — "Cut v1.2.0" deleted as done; issue #4 struck item deleted per the delete-done rule; harvest routed from `docs/status/2026-09-22_21-46`, both `2026-09-23_00-04` reports, and `docs/planning/2026-09-22_23-04`; the go-1.27.1 directive repair + CHANGELOG link-block repair + CI 1.27.x alignment landed in the same pass)._

---

## High Priority

- [ ] **Decide and cut the next release: `v1.3.1` patch vs `v1.4.0` minor** — `[Unreleased]` carries the `etagmetrics` removal (+ HitRatio correction-of-record), the Godoc examples overhaul, the `server.go` Go-1.27 exhaustruct fix, and the go-etag v0.5.0 / Go 1.27.1 floor. A removal plus a dependency/toolchain floor suggests a minor; the correction-of-record alone would be a patch. Then RELEASE.md steps (incl. step 12.5: CI green on the exact tag commit), plus the pre-tag art-dupl sweep (`-t 2..25` zero floor and `-t 1` exactly-one-accepted-group baseline, both documented in AGENTS.md Commands). Timing is owner-gated. Sources: docs/status/2026-09-23_00-04_etagmetrics c2/f14, docs/status/2026-09-22_21-46 f17, CHANGELOG `[Unreleased]`.
- [ ] **Verify CI green on master after the 2026-09-23 go-directive repair** — post-v1.3.0 master carried `go 1.27` alongside go-etag v0.5.0 (daemon casualty, unbuildable for local toolchain resolution; fixed to `go 1.27.1` in go.mod/go.work with the CI matrix bumped to `1.27.x`). Confirm the repair commit's run is green and that the red window is explained by exactly this. Source: this session's fix; docs/status/2026-09-23_00-04_etagmetrics f20.

## Medium Priority

- [ ] **Example gap batch (superb-examples follow-through)** — `httpspec` has no example for its flagship `Run(t, handler)` (highest godoc value); a composition-walkthrough example (RequestID → Recovery → CORS on a real mux) and semantic deep-cut examples are deferred from the audit: `Nonce`×`SecurityHeaders` ordering, `CORSConfig.AllowPrivateNetwork` end-to-end, `KeyedRateLimiter` `MaxKeys`/`EvictionTTL` eviction, `Decompression` `MaxDecompressionSize` zero-value, error-model demos beyond routing (`errors.Is` sentinel match, `Classify` retry decision), a `ClientIP` proxy-trust-caveat example (security-shaped examples teach the footgun), and a `ResponseRecorder` `Status() == 0` vs `WroteHeader()` distinction demo, plus an `ExampleCompression` variant showing `AbsentEncodingFirstConfigured` (currently prose-only in the README). Sources: docs/planning/2026-09-22_23-04 c)-lists, docs/status/2026-09-23_00-04_superb c1–c4/f5/f8/f13–f16/f28–f29/f42–f43.
- [ ] **Pin LNA header behavior on denied-origin preflights** — with `DenyUnmatched` withholding ACAO, the preflight 204 still carries `Access-Control-Allow-Private-Network: true` (browser fails at ACAO first, so no capability is granted). The four existing tests (`cors_private_network_test.go`) cover allowed/default/actual-request/passthrough — none pins the denied-origin case. Add the test + one field-doc sentence, or decide to suppress when the origin is denied (owner security-posture question pending). Source: docs/status/2026-09-15_05-47 f2/g3.
- [ ] **httpspec: opt-in LNA preflight spec** — `CORSSpecs()` ships 5 specs; an opt-in Local Network Access preflight spec (OPTIONS + request headers → 204 + LNA header when configured) fits the `WithExtraSpecs` pattern. Source: docs/status/2026-09-15_05-47 f4.
- [ ] **CSRF SameSite-fallback hardening trio** — the 2026-09-15 fallback shipped with its hardening pass deferred (verified still absent 2026-09-23: no `withSecureFallback` mutation tests exist): mutation-check the 6 fallback tests (bypass `withSecureFallback` → fallback tests must fail) plus the rewritten `TestChain_NonceInnerToSecurityHeaders_OverwritesStaticCSP` from the same window, `-race -coverprofile` probe of the new branch, 30–60s smoke-fuzz of `FuzzCSRFMiddleware_OriginHeaders` + `FuzzCSRFMiddleware_TokenValidation`, `server_timing` sub-module gate, full `nix flake check`. Sources: docs/status/2026-09-15_07-00 b1/f2/f3/f5/f8/f9, docs/status/2026-09-15_06-14 b3/f4, docs/status/2026-09-14_18-14 B2/f1.
- [ ] **Confirm the B1+opt-out interpretation of the 2026-09-15 owner ruling** — "good defaults" shipped as `SameSite=None` → `Secure=true` fallback (NOT the `Lax` default). If the intended default was the Lax fallback, flip `withSecureFallback`, the tests, and the migration note. One word from the owner decides. Source: docs/status/2026-09-15_07-00 g1/f11.
- [ ] **Extract response compression into `go-compression`** — full Pareto plan: [docs/planning/2026-08-16_08-03_extract-compression-into-go-compression.md](docs/planning/2026-08-16_08-03_extract-compression-into-go-compression.md). Trigger: go-datastar needs SSE-safe compression. **Deferred post-v1.1 (decision 2026-09-10):** the `writerPool` resettability-probe refactor invalidated the plan's line-by-line inventory — the plan's own anti-Verschlimmbesserung guardrail requires a fresh inventory pass before execution. Preconditions: (1) refresh the plan inventory, (2) new-repo/remote setup by the owner, (3) execute per the plan's phased-commit discipline.
- [ ] **Re-run `architecture-review` post-v1.1** — the last full run predates the ETag extraction, keyed limiter, compose API, attestation defense + XFP trust model, the v1.1.0 removals, LNA, the SameSite fallback, pattern propagation, and the etagmetrics add/remove cycle. Sources: docs/status/2026-09-15_06-14 f15, docs/status/2026-09-10_04-03 c4.

## Low Priority

- [ ] **httpspec discovery push (docs-site page remains)** — README section + runnable example landed 2026-09-11; the importer scan still shows zero public importers, and private consumer repos exist. Remaining: a docs-site page for the spec runner (blocked on the website-launch effort). Owner decision: push discovery, not retire.
- [ ] **Verify the LNA header contract against live Chrome/spec docs** — the LNA session's web verification failed twice (tool error); implementation rests on dnsblockd's Chromium-unittest-cited local contract. One web pass over current Chrome LNA docs confirms or flags spec churn. Source: docs/status/2026-09-15_05-47 b1/f3.
- [ ] **LNA documentation polish** — `Example*` with `// Output:` for AllowPrivateNetwork (testableexamples-compliant; the README prose + field-table rows landed with v1.2.0), and a field-doc note on the Max-Age × LNA preflight-caching interplay. Source: docs/status/2026-09-15_05-47 f9/f10/f14.
- [ ] **LNA verification polish** — mutation-check the 4 new CORS tests (one-command mutate-run-revert so the daemon never sees the mutation), coverage probe for the new branch under `-race -coverprofile`, an LNA-shaped fuzz seed, and a `server_timing` sub-module gate run for completeness. Source: docs/status/2026-09-15_05-47 f5/f11/f12/f13.
- [ ] **Release-engineering follow-ups** — (1) gate `release.yml` on CI success (workflow_run or single-publisher design) so a red tag cannot publish silently; (2) add a lychee gate to CI/pre-commit (links at 0 since 2026-09-11 — keep them there); (3) fix the dev-mode lychee no-op (`exec: lychee: not found` via the `nix develop -c` fallback counts as success): add `lychee` to the flake devShell (verified still absent 2026-09-23) or make BuildFlow's fallback loud. Sources: docs/status/2026-09-11_13-49 f7, docs/status/2026-09-11_05-37 f13, docs/status/2026-09-11_06-16 f1/g2.
- [ ] **Export `csrf.trusted_origin_invalid` as an exported `Code` constant?** — consumers currently match it via the string; exporting is additive and frozen-API-safe (verified unexported 2026-09-23). Owner decision, standing since 2026-09-14. Sources: docs/status/2026-09-15_07-00 f21, docs/status/2026-09-14_18-14 f9/g3.
- [ ] **MD060 table-style ruling + buildflow result-cache purge** — standardize status/living docs on the single-space compact style (record in AGENTS.md) or add MD060 to the judged-disable list (verified not disabled 2026-09-23); either way purge the buildflow result-cache rows (7-day TTL replay). Owner style ruling. Source: docs/status/2026-09-15_06-14 b5/f6/e3.
- [ ] **CSRF docs polish batch** — README `SameSite=None` fallback prose paragraph next to the config table; a testable `Example*` for the fallback (`// Output:` per testableexamples); a "verbatim, no fallback" warning on `ConfigureNosurfHandler`'s doc comment; audit `CSRFResponseHeaderMiddleware` for cookie-writing paths and pin "cannot emit cookies" if untested. Sources: docs/status/2026-09-15_07-00 f13/f14/f16/f17.
- [ ] **CSRF test-depth batch** — opaque-URL TrustedOrigins entries pinned by unit test (`mailto:`, `data:`); broken-list fallback-log assertion; `ValidateCSRF` re-invocation idempotency pin; `needsTranslation` branch through `ValidateCSRF` itself; `forwardedProtoFromTrustedProxy` untrusted/empty-header/non-http(s) branches; `requestScheme` TLS fixture; HX-headers token-less fuzz seed; `errors.Is(err, ErrCSRFConfig)` chain assertions for the remaining Validate branches; a CSRF middleware benchmark row (attestation check is hot-path for unsafe methods). Sources: docs/status/2026-09-14_18-14 f5/f6/f21–f29/f35, docs/status/2026-09-14_17-14 f21–f29/f40.
- [ ] **go-error-family#5 follow-through** — issue filed and verified (exists, open); `gh issue view` rendering sanity done 2026-09-15. Remaining: implement the upstream "Conditional requests" docs section if accepted; if accepted, sweep the httputil README + architecture-reference classification tables with a link to the new guidance. Sources: docs/status/2026-09-15_06-14 b6/f7/f38.
- [ ] **jsonv2 stabilization follow-through** — bump go.mod when stdlib lands `encoding/json/v2` non-experimental (Go ≥ 1.28 expected) and drop `GOEXPERIMENT=jsonv2` from the documented erraudit commands in one change (DECISION_LOG 2026-09-11; clears the standing gopls `stdversion` warnings). Source: docs/status/2026-09-15_06-14 c12.
- [ ] **Write the external-claim raw-extraction patterns into a reference** — `gh api repos/<o>/<r>/contents/<path>` for raw markdown, `download`+grep for large specs, `mdn/browser-compat-data` JSON for version claims; proven in the 2026-09-15 sessions so the next `agentic_fetch` outage costs nothing. Also: report/fix the `agentic_fetch` json-unmarshal failure (environment tooling). Sources: docs/status/2026-09-15_07-00 f32/f33/e3.
- [ ] **go-etag v0.5.0 ecosystem verification** — confirm the module proxy + pkg.go.dev serve `go-etag/metrics`; smoke `go get` in a scratch consumer; and (in that repo) run the deferred gates: `go vet`, the documented erraudit gate, a `-race -count=10` soak, and a `metrics/` coverage measurement. Sources: docs/status/2026-09-23_00-04_etagmetrics f1–f7, docs/status/2026-09-23_00-04_superb f32.
- [ ] **Add the scripts/examples review sweep as a standing pre-release checklist row** — the 2026-09-15 re-run found three real findings despite a prior "clean" claim; the scope belongs in RELEASE.md's checklist rather than depending on lost review passes being re-noticed. Source: docs/status/2026-09-15_06-14 e9/f18.
- [ ] **`writeHealthBody` `_ =` discard — documented honest-silence vs propagation** — the health handler's post-header-commit body write discards its error by design; owner call whether it joins the documented honest-silence set explicitly or gains a log line. Standing since the 2026-09-11 release session. Source: docs/status/2026-09-11_13-49 f2.
- [ ] **Consolidate the erraudit-residual documentation** — the topic lives in three places (Commands-block verdict, BuildFlow "Residual detect-only findings" bullet, Non-Obvious Behaviors honest-silence paragraph); one canonical home + cross-references. Source: docs/status/2026-09-11_10-03 c3/f6.
- [ ] **Purge buildflow result-cache rows for the resolved exhaustruct + ghost-module findings** — first check whether the 7-day TTL already expired them (post-etagmetrics-removal runs should be clean); purge surgically only if replays persist (the documented sqlite pattern in AGENTS.md). Sources: docs/status/2026-09-23_00-04_superb f19/f40.
- [ ] **Fleet grep for `etagmetrics` pseudo-version consumers** — sweep `go.sum` across `~/projects` for anything that ever consumed `httputil/etagmetrics` on a master pseudo-version; migrate any hit to `go-etag/metrics`. Source: docs/status/2026-09-23_00-04_etagmetrics f25.

## Post-v1.1 (frozen-API or v2.0 material — recorded, not fixed, in the 2026-09-11 full-code review)

- [ ] **`ValidateCSRF` returns `*httptest.ResponseRecorder`** from a production API (test-support type, hidden behaviors). Frozen v1.0 symbol; a result-type change is a signature change → v2.0 material. (Review finding 1.)
- [ ] **`MiddlewareFunc` vs `Middleware` alias split** — inline literals silently lack `.Then`; making `MiddlewareFunc` canonical deprecates the core alias → v2.0 discussion. (Review finding 7.)
- [ ] **Typed `MiddlewareStack` names** — `Add` takes untyped `string` names despite the `Middleware*` constant set; typed names are an additive candidate (new type + overload). `Build`-without-validate is documented and relied upon. (Review finding 8.)
- [ ] **Pool-contract hardening** — `compress_pool.go` probe doesn't check a `(nil, nil)` factory return, `acquire`'s factory parameter is ignored on the pooled path, `release` has no provenance check. Internal-contract hardening for the pool's next touch. (Review finding 9.)

---

_Long-term vision and raw ideas live in [ROADMAP.md](ROADMAP.md). Completed work is recorded in [CHANGELOG.md](CHANGELOG.md). Historical open-item evidence lives in the inline `done at` markers of `docs/status/2026-*.md`; this list is the live backlog._
