# TODO List — httputil

Short- and mid-term improvement tasks. Each item verified against the actual code. Completed work lives in [CHANGELOG.md](CHANGELOG.md); rejected ideas live in [ROADMAP.md](ROADMAP.md) Non-goals; process decisions live in [docs/DECISION_LOG.md](docs/DECISION_LOG.md).

_Updated: 2026-09-10 (v1.0 completion sweep: full backlog executed, v1.0.0 cut locally)._

---

## High Priority

- [ ] **Push the v1.0.0 tag** — the release is cut locally: CHANGELOG frozen as `[1.0.0] - 2026-09-10`, annotated tag on the release-prep commit. Remaining: `git push origin master && git push origin v1.0.0`, then per [docs/RELEASE.md](docs/RELEASE.md) §5 verify pkg.go.dev + `go get`. Recommended first: the scheduled full-code-review below (substantial code landed in the 2026-09-10 sweep). Admission contract CONFIRMED 2026-09-10: documented on `Check`, `KeyedRateLimiterMiddleware`, and the migration guide; `Wait(ctx)` post-v1.0 path in ROADMAP. Sources: `23-08:f7`, ROADMAP "v1.0", `11-30:f4–f7`.
- [x] **Verify the CSRF `Sec-Fetch-Site` trust model against nosurf before v1.0** — DONE 2026-09-10: verified at nosurf v1.2.0 source (`ensureSameOrigin` short-circuits on any literal same-origin attestation); hardened with `ErrCSRFAttestationConflict` contradiction rejection, trust model documented in `csrf.go` + README, and the httpspec no-injection-header-reflection spec added.
- [x] **Decide `CompressionConfig.Level = 0` semantics** — DONE 2026-09-10: 0 means "unset → gzip.DefaultCompression" everywhere; pinned by `TestCompression_ZeroLevelMeansDefaultCompression`.

## Medium Priority

- [ ] **Extract response compression into `go-compression`** — full Pareto plan: [docs/planning/2026-08-16_08-03_extract-compression-into-go-compression.md](docs/planning/2026-08-16_08-03_extract-compression-into-go-compression.md). Trigger: go-datastar needs SSE-safe compression. Decompression stays in httputil. **Deferred post-v1.0 (decision 2026-09-10):** the v1.0 cut takes precedence (the plan moves ~3,400 LOC and touches three repos — it is not pre-v1.0 work), and the 2026-09-10 `writerPool` resettability-probe refactor invalidated the plan's line-by-line file inventory; the plan's own anti-Verschlimmbesserung guardrail requires a fresh inventory pass against the current code before execution. Preconditions: (1) refresh the plan inventory, (2) new-repo/remote setup by the owner, (3) execute per the plan's phased-commit discipline.
- [x] **CI release workflow** — DONE 2026-09-10: `release.yml` now runs build + vet + race tests for BOTH modules, lint, and govulncheck for both modules (root + `server_timing`), then the GitHub Release. Toolchain pins aligned to go1.26.7.
- [x] **go-error-family upstream: conditional-request classification** — DONE 2026-09-10 (verified draft): all verify-before-filing gates passed (no existing guidance in source/README/TODO_LIST/ROADMAP/FEATURES; `familyData` covers only 400/409/503/500; the `HTTPStatuser` escape hatch exists but is undocumented for this case). Verified issue draft: [docs/planning/2026-09-10_go-error-family-conditional-request-classification-issue-draft.md](docs/planning/2026-09-10_go-error-family-conditional-request-classification-issue-draft.md). Remaining: owner files it (own repo).
- [x] **Refresh `docs/benchmarks.md` rows for benches changed 2026-08-30** — DONE 2026-09-10: full 3s×5 re-measure on the post-sweep code; all moved-recorders/ReportAllocs/eviction-path rows updated; stale pre-`b.Loop` `b.ResetTimer` calls removed while dissolving `bench_batch_test.go`.
- [x] **`CSRFConfig.Validate` side-effect cleanup** — DONE 2026-09-10 (landed in v1.0.0, the last cheap moment): `Validate()` is now pure (value receiver, no logging, no mutation — validated CIDR entries without storing results); CIDR parsing moved to construction via `CSRFConfig.withParsedTrustedProxies()` (any parse failure yields an empty CIDR list — safer than the old partial-parse-on-error state); the Secure=false warning moved to the `CSRFMiddleware` constructor; all `CSRFConfig` methods normalized to value receivers. Fuzz target upgraded to cross-check Validate-accepted entries against parse results.

## Low Priority

- [x] **Document the `Compose` bundle pattern** — DONE 2026-09-10: [docs/integrations/compose-bundles.md](docs/integrations/compose-bundles.md) (build-once-apply-many bundles, stack nesting via `MiddlewareStack.Middleware()`, ordering rules, `MiddlewareFunc.Then` wiring contract), linked from README.
- [x] **KeyedRateLimiter property test** — DONE 2026-09-10: `TestPerKeyLimiter_Property_CapacityEvictionPreservesInvariants` (2,000 seeded ops, heap/map/heapRef/index/ordering invariants checked every 97 ops) + `TestPerKeyLimiter_MaxKeysChurn_KeepsMostRecentKeys` (sliding-window determinism) + `BenchmarkKeyedRateLimiter_MaxKeysChurn` (fresh key per iteration = real eviction path).
- [x] **CORS fuzz invariant for exact-origin allowlists** — DONE 2026-09-10: `FuzzCORSOriginEcho` — no third-party reflection ever; exact-allowlist+DenyUnmatched oracle (echo ⟺ allowlisted); ~1.4M execs clean.
- [x] **Decide `WrapConflict`/`WrapOrchestration` symmetry** — DONE 2026-09-10: asymmetry was an oversight, not intent; both added to `Code` (constructor + Wrap pairs now cover all six families), with cause/family tests.
- [x] **Skip pool `Get` for non-resettable factories** — DONE 2026-09-10: `writerPool` probes the factory once at construction; non-resettable factories skip the pool entirely (no wasted pooled allocation per request); deterministic tests for skip/probe/Rebind/release-drop paths.
- [x] **`Server.Addr()` resolved-port variant** — DONE 2026-09-10: API decided and shipped as `Server.ListenerAddr() (net.Addr, bool)`; `Start`/`StartTLS` now `net.Listen` first (bind errors surface immediately on the error channel) and track the listener; cleared on successful `Shutdown`. TLS test switched to `:0` + `ListenerAddr` (killed the `reserveFreePort` port-reuse race).
- [x] **Negotiator wire-format fuzz target** — DONE 2026-09-10: `FuzzNegotiatorWireFormat` (~970K execs clean): negotiation with identity registered always succeeds; results are registered canonical encodings with q ∈ (0,1]. Gzip multistream reliance documented in the `FuzzCompression` round-trip comment.
- [x] **Test-helper hygiene trio** — DONE 2026-09-10: `bench_batch_test.go` dissolved into per-middleware bench files (id_generator/csrf/server/ratelimit_keyed; stale `b.ResetTimer`s dropped); `waitForTLS` consolidated into `testutil_test.go` beside `waitForServerStart`, now fail-fast on startup errors and dialing the resolved addr; `reserveFreePort` deleted (obsolete via `ListenerAddr`); TLS test cert switched RSA-2048 → Ed25519.
- [x] **flake.nix benchmark protocol app** — DONE 2026-09-10: `nix run .#bench` executes the documented 3s×5 protocol; `dprint` added to the devShell (markdown formatter verification no longer skipped).
- [x] **Slim AGENTS.md below the 30 KB docs-health budget** — DONE 2026-09-10: 58.5 KB → 29.3 KiB. File-export tables + Error Classification + lint profile moved to [docs/architecture-reference.md](docs/architecture-reference.md) (AGENTS keeps pointers); Hard Constraints, Non-Obvious Behaviors, and Testing Conventions intact.
- [x] **Test-coverage gaps: MaxBodySize bench/fuzz + five missing examples** — DONE 2026-09-10: `FuzzMaxBodySize` (pins the probed stdlib contract: error ⟺ `len(body) > max(limit,0)`; discovered negative limits let the empty body through) + `BenchmarkMaxBodySize`; examples added for `Metrics`, `HealthHandler`, `Server` (with `ListenerAddr`), `MiddlewareStack`. `ExampleRateLimit` formally declined — the deprecated API should not get a discoverable example.
- [x] **Nonce design decisions + composition-test cluster** — DONE 2026-09-10: formally declined `NonceConfig.Generator` and public `GenerateNonce` (rationale in `nonce.go` doc + DECISION_LOG; `WithNonce` is the injection escape hatch); four never-run composition tests added (nonce × Compression round-trip with CSP-nonce cross-check, nonce × CORS header coexistence, nonce × ServerTiming wrapper, nonce × CSRF token helpers).
- [x] **ID-generator refill-path benchmark** — DONE 2026-09-10: `BenchmarkIDGeneratorRefillSwap` (alloc + crypto/rand + atomic publish, `ns/ID-amortized` metric) vs `BenchmarkIDGeneratorRefillRawRandRead` baseline; swap overhead ≈ 2 ns/ID on top of the ~17.5 ns/ID syscall amortization.
- [x] **Rework the ID-generator random buffer into a generation-swapped ring** — DONE 2026-09-10: published generation buffers are immutable; claims are monotonic and generation-stamped (slot s → generation s/256, offset s%256) so no slot is ever drawn twice and no claim can duplicate a tail across generations; superseded generations are GC-reclaimed while slow readers finish copying. The formal copy-vs-refill data race is gone (worst case was a torn read of two independent crypto/rand fills). Stress test: 8 goroutines × 20k IDs under `-race`, asserting BOTH full-ID and random-tail uniqueness. Decision recorded in DECISION_LOG.
- [x] **dprint availability in the dev environment** — DONE 2026-09-10: `pkgs.dprint` in the flake devShell (verified 0.56.1 on PATH).
- [x] **Nightly fuzz: add a crash issue-template step** — DONE 2026-09-10: `nightly-fuzz.yml` files an issue on failure (run-log link + testdata/fuzz pointer, `issues: write` permission, label fallback) and runs the three new fuzz targets.
- [x] **govulncheck for `server_timing` in CI** — DONE 2026-09-10: both `ci.yml` and `release.yml` scan both modules.
- [x] **Commit-lint CI step** — DONE 2026-09-10: `commit-lint` job on PRs runs [scripts/check-commit-lint.sh](scripts/check-commit-lint.sh) (conventional-prefix regex over `base..head`).
- [x] **CI/release tooling extras** — DONE 2026-09-10: `scripts/coverage-threshold` (Go, fails loudly on malformed reports; library-only coverage — the dev-tooling package is excluded) replaces the fragile awk; [docs/RELEASE.md](docs/RELEASE.md) + [scripts/prerelease-check.sh](scripts/prerelease-check.sh) automate the nine release gates.
- [x] **Resolve `go.work` `go 1.26.5` vs CI `1.26.x` vs local `1.26.7` version pinning** — DONE 2026-09-10: go.work/go.mod had already converged on 1.26.7; CI's `GOTOOLCHAIN: go1.26.6` pin was now OLDER than the module requirement (a broken CI) — bumped to go1.26.7 in `ci.yml` and `release.yml`. Policy: go.mod/go.work declare the minimum; CI pins that same patch for govulncheck currency; local tracks the latest patch.
- [x] **`TestChain_DecompressionThenMaxBodySize` 417-as-signal assertion** — DONE 2026-09-10: the test now asserts the classified `errDecompressionSizeExceeded` sentinel directly instead of treating the handler's 417 status as the signal.
- [x] **Post-v1.0: revisit `KeyExtractor` returning `""`** — DONE 2026-09-10 (docs verified unambiguous, no change needed): `""` = exempt/always-allowed vs nil extractor = shared single global key are documented distinctly on the type and config field (`ratelimit_keyed.go`).
- [x] **Integration-docs content refresh** — DONE 2026-09-10: samber-do (API usage verified current), huma + prometheus (verified), brotli-zstd (pool-skip semantics added), redis (deprecation notice corrected to post-v1.0 removal).
- [ ] **Schedule the next `full-code-review`** — DUE NOW: the 2026-09-10 sweep landed substantial code (Server listener tracking, writerPool probe, ID-generator ring, CSRF Validate purity, ~30 tests/fuzz/bench targets) — per this item's own rule ("re-run before v1.0 if substantial code lands") the review should run BEFORE the v1.0.0 tag is pushed. Schedule: immediately; scope: everything in the `[1.0.0]` changelog section.

---

_Long-term vision and raw ideas live in [ROADMAP.md](ROADMAP.md). Completed work is recorded in [CHANGELOG.md](CHANGELOG.md). Historical open-item evidence lives in the `## Resolution` appendices of `docs/status/2026-*.md`; this list is the live backlog._
